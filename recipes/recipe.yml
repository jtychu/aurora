---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: aurora
# description will be included in the image's metadata
description: Personalized image of Aurora DX.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/aurora-dx
image-version: latest # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: script
    scripts:
      - remove-repos.sh

  - type: rpm-ostree
    repos:
      # Default
      - https://copr.fedorainfracloud.org/coprs/atim/starship/repo/fedora-%OS_VERSION%/atim-starship-fedora-%OS_VERSION%.repo

      - https://www.scootersoftware.com/files/scootersoftware.repo
    install:
      # Default
      - micro
      - starship
      
      - bcompare
    remove:
      - input-remapper
      - kate
      - kate-krunner-plugin
      - kate-libs
      - kate-plugins
      - kwrite
      - sysprof

  - type: default-flatpaks
    configurations:
    - scope: system
      notify: true # Send notification after install/uninstall is finished (true/false)
      # If no repo information is specified, Flathub will be used by default
      install:
        # From https://github.com/ublue-os/aurora/blob/main/flatpaks/system-flatpaks.list
        - com.github.tchx84.Flatseal
        - com.ranfdev.DistroShelf
        - io.github.flattool.Warehouse
        - io.github.kolunmi.Bazaar
        - io.missioncenter.MissionCenter
        - org.deskflow.deskflow
        - org.fedoraproject.MediaWriter
        - org.fkoehler.KTailctl
        - org.kde.gwenview
        - org.kde.kcalc
        - org.kde.kclock
        - org.kde.kontact
        - org.kde.kweather
        - org.kde.okular
        - org.kde.skanpage
        - org.mozilla.firefox

        # From https://github.com/ublue-os/aurora/blob/main/flatpaks/system-flatpaks-dx.list
        - io.podman_desktop.PodmanDesktop
        - me.iepure.devtoolbox
        - sh.loft.devpod

        - com.discordapp.Discord
        - com.jetpackduba.Gitnuro
        - com.protonvpn.www
        - com.ultimaker.cura
        - org.libreoffice.LibreOffice
        - org.videolan.VLC

  - type: bling
    install:
      - 1password

  - type: brew
    brew-analytics: false

  - type: justfiles
    include:
      - install-youtube-music-flatpak.just
      - setup-1password-flatpak-browser-integration.just
      - setup-git-global-identity.just
      - setup-hostname.just

  - type: signing # this sets up the proper policy & signing files for signed images to work fully

